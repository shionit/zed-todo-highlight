[package]
name = "todo-highlight-extension"
version = "0.2.2"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Exact pin — WASM ABI changes in minor bumps can silently break the extension.
zed_extension_api = "=0.7.0"
